#ifndef SUM_H
#define SUM_H

/**
 * main - Header file that defines a function-like macro SUM(x, y)
 * that computes
 * @x: sum of the number.
 * @y: the sum of macro
 */

#define SUM(x, y) ((x) + (y))

#endif
