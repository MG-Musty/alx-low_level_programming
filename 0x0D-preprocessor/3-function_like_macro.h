#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H


/**
 * main - Header file that defines
 * that computes the absolute
 * @abs: function of macro
 * @x: value of number
 */
#define ABS(x) ((x) < 0 ? (-x) : (x))

#endif
